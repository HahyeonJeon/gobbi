/**
 * Artifact selector — selects prior-step artifacts from the session directory
 * for inclusion in the current step's compiled prompt.
 *
 * Skeleton file — A.9 populates this module with the concrete selection logic
 * (first-step → no artifacts; later steps → prior steps' artifacts in order).
 */

export const __todoArtifactSelector: unique symbol = Symbol('todo.artifact-selector');

/**
 * Skill-content loader — reads `.claude/skills/` content into ordered
 * `StaticSection[]` arrays for inclusion in compiled prompts.
 *
 * Skeleton file — A.8 populates this module with the concrete loader that
 * consumes `_gotcha`, `_claude`, `_git`, `_skills`, `_agents`, `_rules`,
 * `_project`, `_notification`, and `_gobbi-cli`.
 */

export const __todoSkills: unique symbol = Symbol('todo.skills');

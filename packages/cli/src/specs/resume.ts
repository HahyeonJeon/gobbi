/**
 * Full-replacement resume prompt compiler.
 *
 * Skeleton file — PR D (subtask D.4) populates this module with
 * `compileResumePrompt(state, store)`, which produces a standalone
 * recovery prompt (NOT a suffix-append to the prior prompt) per the
 * user-locked design revision. The stub export here gives A.4's
 * `index.ts` a stable import target.
 */

export const __todoResume: unique symbol = Symbol('todo.resume');

export function compileResumePrompt(): never {
  throw new Error('not implemented — populated in PR D');
}

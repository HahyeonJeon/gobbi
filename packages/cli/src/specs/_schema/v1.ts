/**
 * StepSpec JSON Schema v1 — TypeScript binding.
 *
 * Skeleton file — A.6 populates this module with the `JSONSchemaType<StepSpec>`
 * binding that ties the runtime ajv schema in `./v1.json` to the TypeScript
 * `StepSpec` interface from `../types.ts` (created by A.3b).
 */

export type StepSpecSchema_v1_Todo = unknown;

/**
 * Token budget allocator.
 *
 * Skeleton file — A.5 populates this module with the two-pass floor-then-
 * proportional allocator that respects per-section minimums and enforces
 * whole-section inclusion (never mid-truncation).
 */

export const __todoBudget: unique symbol = Symbol('todo.budget');

/**
 * Section types, brand symbols, factory functions, and content-hash helpers.
 *
 * Skeleton file — A.3 populates this module with the symbol-keyed section
 * brand types, factory constructors, variadic tuple compile signature, and
 * deterministic `contentHash(section)` helper.
 */

export const __todoSections: unique symbol = Symbol('todo.sections');

/**
 * Prompt assembly — `compile()` entry point and cache-ordered section assembly.
 *
 * Skeleton file — A.4 populates this module with the `compile()` function,
 * cache-ordered prompt assembly, and content linter hooks.
 */

export const __todoAssembly: unique symbol = Symbol('todo.assembly');

---
name: cli-platform
description: "MUST load when looking up or diagnosing current command-line execution-platform facts about terminals, standard streams, process lifecycle, pipes, signals, shell entry, path resolution, encoding, locale, or operating-system differences for a line-oriented CLI."
allowed-tools: Read, Grep, Glob, Bash, WebSearch, WebFetch
skill-type: tool
---

# CLI Platform

This nonproduction skeleton reserves one lookup Manual for current line-oriented command execution-platform
facts. It owns bounded Available, Unavailable, or Unknown facts, never product support policy.

A fresh Tool Skill Writing pass will replace every placeholder below. It must retain this trigger, bind exact
tuples and dated evidence, and route Bun, compiler, package, and delivery mechanisms to existing owners.

## Principles

### Keep fact lookup bounded

The final principles will govern exact tuple binding, current evidence, direct observation, limitations, and
honest unknowns without widening a fact into a product judgment.

## Rules

- **MUST replace this nonproduction skeleton through the assigned fresh tool pass.** The final skill must
  preserve the canonical trigger and one-subject fact boundary.

## Manual

### Reserved lookup surface

The final Manual will cover terminal, standard-stream, process, pipe, signal, shell-entry, path-resolution,
encoding, locale, operating-system, evidence, conflict, limitation, diagnosis, and refresh lookups.

Fact consumers are `cli-architecture`, `cli-interface`, `cli-security`, `cli-development`, and `cli-release`.
Bun compilation and named-runtime mechanisms route to `typescript-toolchain`; package and direct-unit
mechanisms route to `typescript-packaging` and `typescript-cli-delivery`.

## References

- [CLI Platform checklist](checklists.md)

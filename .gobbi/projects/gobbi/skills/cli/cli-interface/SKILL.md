---
name: cli-interface
description: "MUST load when choosing or reviewing help and discovery content, terminal wording, human, plain, or structured rendering within accepted stream roles, diagnostics, prompts, progress, visualization, accessibility, localization, or adaptive presentation for a line-oriented CLI."
allowed-tools: Read, Grep, Glob, WebSearch, WebFetch
skill-type: preference
---

# CLI Interface

This nonproduction skeleton reserves the human and automation expression contract for a line-oriented CLI.
It owns expression within accepted semantic channels, not command meaning, support, or implementation.

A fresh Preference Skill Writing pass will replace every placeholder below. It must retain this trigger and
the line-oriented boundary, including the exclusion of automatic paging and full-screen terminal interfaces.

## Principles

### Preserve meaning across expression choices

The final principles will govern human, plain, structured, accessible, localized, and adaptive expression
without changing Architecture-owned semantics or stream roles.

## Rules

- **MUST replace this nonproduction skeleton through the assigned fresh preference pass.** The final skill
  must preserve the canonical trigger and accepted expression boundary.

## Preferences

### Reserved judgment surface

The final preferences will cover help, wording, human, plain, JSON, JSONL, diagnostics, prompts, progress,
line-oriented visualization, accessibility, localization, and adaptive presentation.

Meaning and stream roles route to `cli-architecture`; current facts route to `cli-platform`; trust
requirements route to `cli-security`; coordination routes to `cli-development`; support routes to
`cli-release`; process evidence routes to `typescript-testing`.

## References

- [CLI Interface checklist](checklists.md)

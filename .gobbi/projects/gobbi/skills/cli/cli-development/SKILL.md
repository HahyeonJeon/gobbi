---
name: cli-development
description: "MUST load when coordinating a scoped change to a line-oriented TypeScript CLI across accepted product contracts, security analysis, TypeScript implementation and testing, Bun and platform facts, package or direct delivery, release judgment, authorized external action, post-release observation, or maintenance."
allowed-tools: Read, Grep, Glob, Bash, Write, Edit, WebSearch, WebFetch
skill-type: operation
---

# CLI Development

This nonproduction skeleton reserves the coordinator operation for a scoped multi-owner CLI change. It owns
current lifecycle state, agreement, blockers, authority stops, cold handoffs, and maintenance coordination.

A fresh Operation Skill Writing pass will replace every placeholder below. It must route specialist work to
its owner and must not implement, test, package, deliver, release, or operate merely because it coordinates.

## Principles

### Keep coordination separate from specialist work

The final principles will govern owner-result agreement, dependency state, contradiction, authority, and
recoverable handoff without copying specialist policy.

## Rules

- **MUST replace this nonproduction skeleton through the assigned fresh operation pass.** The final skill
  must preserve the canonical trigger and accepted coordinator boundary.

## Procedure

### Phase 1 — Bind the scoped coordination subject

#### 1.1 Reserve the lifecycle contract

- Bind accepted results from `cli-architecture`, `cli-interface`, `cli-platform`, `cli-security`, and
  `cli-release` when their triggers apply.
- Route implementation, toolchain, evidence, package-backed artifacts, and direct units to the applicable
  `typescript-development`, `typescript-typing`, `typescript-toolchain`, `typescript-testing`,
  `typescript-packaging`, and `typescript-cli-delivery` owners.
- Reserve current-state, blocker, stale-evidence, authority, cold-handoff, observation, maintenance,
  deprecation, and retirement outputs for the final Procedure.
- Stop at the nonproduction skeleton until the assigned fresh pass replaces it.

## References

- [CLI Development checklist](checklists.md)

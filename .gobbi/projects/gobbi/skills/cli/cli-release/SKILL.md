---
name: cli-release
description: "MUST load when choosing or reviewing supported runtime, target, or delivery policy, required consumer evidence, runtime, artifact, installation, or data compatibility, release readiness, rollout controls, recovery, rollback, forward-fix, support, deprecation, or retirement for a line-oriented TypeScript CLI."
allowed-tools: Read, Grep, Glob, Bash, WebSearch, WebFetch
skill-type: preference
---

# CLI Release

This nonproduction skeleton reserves early release policy and later per-tuple readiness judgment for a
line-oriented TypeScript CLI. It owns judgment, not artifact, credential, or external-action mechanisms.

A fresh Preference Skill Writing pass will replace every placeholder below. It must retain this trigger,
separate package and direct identities, and keep Supported, Unsupported, and blocked distinct from facts.

## Principles

### Separate policy, evidence, and action

The final principles will govern exact release promises, evidence sufficiency, readiness, compatibility,
recovery, support, deprecation, and retirement without performing a release action.

## Rules

- **MUST replace this nonproduction skeleton through the assigned fresh preference pass.** The final skill
  must preserve the canonical trigger and judgment-only boundary.

## Preferences

### Reserved early policy result

The final preference will define promised runtime, target, delivery, evidence, compatibility, recovery,
rollout, support, and residual-risk criteria before coordination.

### Reserved later readiness result

The final preference will judge each exact tuple from current `cli-platform` facts, `cli-security` assurance,
the accepted `cli-architecture` and `cli-interface` contracts, and exact consumer evidence coordinated by
`cli-development`. Mechanisms route to `typescript-toolchain`, `typescript-testing`,
`typescript-packaging`, and `typescript-cli-delivery`.

## References

- [CLI Release checklist](checklists.md)

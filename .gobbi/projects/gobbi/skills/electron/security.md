# Electron — Security

**Owns** — the security posture: the **sole** `webPreferences` defaults table; the 20 checklist items on
their two real axes; the `nodeIntegration` coupling rule; the renderer security-warning coverage set with
its numbering-offset table and the disjointness proof for the nine code-only items; the navigation,
redirect, and window-open event surface; the two exact-code obligations; the permission pair; item 5's
origin scoping; session partition and custom-protocol registration; and the fuse posture.

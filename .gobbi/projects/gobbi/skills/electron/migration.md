# Electron — Migration

**Owns** — the upgrade lookup: an opening coverage ceiling and freshness stamp; the single removed-in-vN →
use-X table that is the only home for every removal, deprecation, and rename; the stale-training-data tells;
and the behavior-claim register. This file is a lookup index, not a tutorial.

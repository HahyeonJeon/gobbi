# Electron — Testing

**Owns** — proving the desktop behavior: the Playwright `_electron` entry with the vendor's stability caveat
carried beside it; main-process unit testing through an injected Electron surface; the IPC contract test that
pins both ends of a channel; the adversarial origin tests the origin-scoping rule requires; and the seam for
driving a null and a detached sender frame.

# {Work Name} — Task Hierarchy

> **Document role:** Authoritative task-hierarchy view index<br>
> **Result:** [Coding Planning](../plan-index.md)<br>
> **Reading rule:** Read every part once in the order below. A missing, duplicate, or unlisted task part makes this view incomplete.<br>
> **View boundary:** This view decomposes work. It does not assign agents or define execution order.

## Parts

| Part | Description |
|---|---|
| [tasks-NN.md](tasks-NN.md) | {Replace `NN` with the assigned number and state the work items or complete hierarchy branches in this part.} |

{List every `tasks-NN.md` file exactly once and in depth-first reading order. Keep existing numbers stable;
assign the next unused number to a new part, never rename or reuse a part, and let this index define order.}

# HANDOFF — `go` skill, resuming in a Codex session

**Session:** `2f9595bb-307f-4260-bdd8-8bdb5b6df9ff`
**Branch:** `claude-2026-07-25-2f9595bb-307f-4260-bdd8-8bdb5b6df9ff` (base `develop`)
**Worktree:** `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/worktrees/claude-2026-07-25-2f9595bb-307f-4260-bdd8-8bdb5b6df9ff/`
**Skill dir:** `.gobbi/projects/gobbi/skills/go/` — 18 files, ~4,800 lines
**Stopped at:** Execution task 10 of 11 complete; **task 11 (wiring) NOT started**
**Reason:** token budget. User directive: *"Fix all. But we don't have tokens now."*

---

## 1. Read these first, in this order

1. `.gobbi/projects/gobbi/skills/gobbi/SKILL.md` — rebuild the manager floor (5 skills)
2. `.gobbi/projects/gobbi/skills/codex/SKILL.md` — you are the native-Codex runtime
3. **This file**
4. `…/sessions/2026-07-25-2f9595bb-307f-4260-bdd8-8bdb5b6df9ff/1-ideation/working/iteration-1/open-decisions.md` — **D-1 … D-28**, every locked decision
5. `…/2-planning/outputs/task-plan.md` — the 11-task plan; task 11's contract is what remains
6. `…/3-execution/task-01-o9-fetch-passes/outputs/o9-fetch-ledger.md` — the fetch ledger (**note its retraction block**)
7. `…/3-execution/task-10-tree-sweeps/outputs/sweep-report.md` — 581 lines, per-sweep BEFORE/AFTER

---

## 2. THE WORK TO DO — fix all six findings + three loosenings

An independent V10 probe returned **FAIL**. The **skill's content is sound**; the **checks are weaker
than they appear**. All fixes are in `.gobbi/projects/gobbi/skills/go/checklists.md` unless noted.

### CRITICAL 1 — `GO-CHECK-30` / `V4` is blind to Go's canonical version form

`checklists.md:229`. Anchor `\b1\.27` matches `Go 1.27` but **NOT** `go1.27` nor `` `go1.27` ``.
The tree already carries 20+ `go1.NN` tokens (`go1.26`, `go1.13`, `go1.24`).

**Defeating input, one sentence:** `` Use `go1.27`'s new loop semantics. `` → presents unreleased Go as
current, violates H19, **sweep returns zero hits.**

**Fix:** the `smithy-go` false positive needed a negative lookbehind on `v`, not `\b`. Use
`(?<!v)1\.27` **plus** a `go1\.27` alternation. The file's claim that `\b` "still match[es] that numeral
in every bare, prose, and backticked form" is **false — delete it.**
**Also:** `evaluation.md:244` still states the un-anchored population. The two surfaces disagree; sync them.

### CRITICAL 2 — `GO-CHECK-35` / `V12` has no population and is defeated by paraphrase

`checklists.md:268`, `evaluation.md:252`.

**(a) No declared population.** "for each **sampled** taught claim" — no sample size, no selection rule.
The author picks the sample, so any claim is excludable without an `n/a:<property>` and without evidence
the applicability predicate is false. **Structural CR-6 breach.**
Contrast `GO-CHECK-34`, which correctly declares *"The population is every taught form."* Copy that shape.

**(b) Paraphrase bypasses it.** The operational test is "key term located **and** quoted sentence copied."
A paraphrase has no quoted sentence, so that conjunct is vacuous and the test collapses to substring
presence of a key term.

**Defeating input — this session's own caught fabrication, quote marks removed:**
> `design.md`: "The Go team's guidance is that a generic rewrite performs essentially the same and is
> simpler." `Source: go.dev/blog/when-generics @ 2026-07-26 (raw)`

Real URL, reachable, pinned, right owner, topical, **does not contain the claim** → **PASS**.
V12 caught the real fabrication *only because it was dressed as a verbatim quote.*

**Fix:** declare the population; make the predicate test **claim support**, not quote presence — the
verifier must state which sentence(s) of the fetched source carry the claim, and a paraphrase must be
defensible against those sentences.

### HIGH 3 — `GO-CHECK-27` / `V1`: the exemption list is author-writable

Predicate is `cited + declared_exemptions == total`. **Nothing bounds `declared_exemptions`** or verifies
a declared row actually satisfies class (i) or (ii). Class (ii) is satisfied **by writing the sentence**
"Illustrative sketch — not a taught form" above a taught block. Equation balances → PASS.
The anti-widening statement at `checklists.md:296` sits in **prose, not the predicate** — exhortation,
not a control.
**Composed hole:** `-27` grades that a `Source:` line *exists*; `-35` samples. A block cited to a bogus
owner passes both. **Fix:** give the predicate teeth. **Keep the exemption list empty** — that is the fix's
precondition, not the fix.

### HIGH 4 — `GO-CHECK-33` / `V7` cites two artifacts that do not exist

```
grep -rn "ownership table" *.md  -> 1 hit: checklists.md:242, the reference itself
grep -rn "delta test"     *.md  -> 1 hit: checklists.md:254, the reference itself
```
The gate's second conjunct names *"the ownership table"* — **there is none in the tree.** The run's
population (7 rows) was **invented at scoring time by the author being scored** and is not frozen sweep
data. **The confirmed residual** (padding reusing none of the twelve phrases is invisible) is real and
**nothing else covers it** — its stated mitigation points at the non-existent "delta test."
**Fix:** create the ownership table as frozen data, or drop the conjunct. Do not let the residual's
mitigation sentence stand — it converts an uncovered hole into an apparently-routed one.

### HIGH 5 — `GO-CHECK-29` / `V3`: written predicate ≠ instrument

Predicate says owner-resolution; the scoring script does `elif f in TOOL: tool_ok+=1` — **file membership.**
45 of 231 tokens (19%) cleared on which file they sit in; the child's `**Version / support status:**`
header is never consulted.
**Defeating input:** write *"requires Go 1.31"* in `aws.md` — a Go figure, belongs in the register, no
register row, scores PASS.
**Fix:** consult the header line. **Note D-27's scope decision holds:** the register is Go/toolchain-only;
tool children own their SDK version lines.

### HIGH 6 — `GO-CHECK-28` / `V2`: a "forbidding sentence" can prescribe a retired form

Scoring unit is the sentence; one declared home is "a forbidding sentence." Input:
`` Never call `grpc.Dial`; use `DialContext` instead. `` — both tokens are in the pattern set, both sit in
a sentence containing "Never", **both PASS**, while the sentence prescribes a retired form.
The home test asks whether the sentence forbids *something*, not whether it forbids *this hit*. **Fix that.**

### The three loosening edits — revert or constrain

The sweep executor made 7 pre-run edits to definitions that then scored its own run. Independent ruling:

| # | Edit | Ruling | Action |
|---|---|---|---|
| 3 | `V4` `\b` anchor | **Weakens; its justification is FALSE** | Fix per Critical 1 |
| 2 | `V2` fourth home (`scenarios.md` bad-case fields) | **Weakens** — scoring unit is the whole field, so any retired token anywhere in a `**Situation:**` block passes regardless of framing | Score the field's *sentences*, not the field |
| 5 | `V7` triple reconciliation | **Mixed — loosening in mechanism.** Dropping 3 dead rows and correcting `aws.md` §3 are genuine repairs. But *extending* four fragment cells with the exact tokens from the failing sentences is **clearing a failure by rewriting the answer key**; 6 of 10 BEFORE failures were cleared this way | Encode the distinguishing constraint — *"names a symbol that exists only in that tool's SDK"* — **in the item**, not in the report |

**Sound edits — do not revert:** #6 (`GO-CHECK-34` predicate correction — the old FAIL clause was
unsatisfiable; **best edit of the seven**), #7 (`-03`/`-05` strengthenings), #1 (`V2` allowlist written
down — completes an unresolvable item; caveat: it says "these exact strings" but entry 2 is a class,
an internal contradiction to fix). #4 is **sound as text, unsupported as run** (see HIGH 5).

### Also recorded, lower priority

- **13 checks are universally quantified with no applicability predicate** (`-02,-04,-05,-06,-07,-09,-11,-12,-13,-14,-15,-18,-19`), so "no instances" and "all instances correct" both resolve PASS. The legend requires `n/a:<property>` to cite evidence the predicate is false; the register never says which a vacuous case takes.
- `-31`'s third FAIL disjunct is conjunctive, so an 8th stamp passes when all 7 exist — the FAIL clause is not the complement of "exactly these seven."
- `-34` residuals: a pointer to a file that does not state the floor clears FAIL without satisfying PASS; and the `symlike` extractor cannot reach bare-identifier forms, so an omitted form is invisible rather than unclassifiable.
- `-22` has an approval-based escape ("no prior decision is cited"), violating the file's own header rule at `checklists.md:31-35`.
- `-11` passes an empty `if err != nil {}`; `-06` passes a goroutine handed a `ctx` it never selects on; `-20` passes any skipped gate carrying any unvalidated reason.

**MUST-PRESERVE while fixing:** `GO-CHECK-25` (bidirectional trace) and `GO-CHECK-34`'s population
clause are the two strongest predicates — do not soften either. The header rule *"One positive
acceptance outcome per box"* (`checklists.md:31-35`) is correct and already catches `-22`; enforce it.

---

## 3. THEN: task 11 — wiring, sync, cold-load proof

Contract in `…/2-planning/outputs/task-plan.md`. Summary:

**Wiring — exactly three files, four sites. Verify by pattern match, NOT line number:**
- `.gobbi/projects/gobbi/skills/gobbi/SKILL.md:73` — extend the existing "Language method skills" row's
  link list. **Do not add a row.**
- `.gobbi/projects/gobbi/skills/ideation/SKILL.md:68` and `:174`
- `.gobbi/projects/gobbi/skills/evaluation/SKILL.md:309-310`

**NO other file.** `coding/SKILL.md:293` and `coding/review.md:472` are recorded backlog;
`ideation/evaluation.md:58` is NO-EDIT with reasoning already recorded; `agents/executor.md:37` is a
pre-existing false claim, out of scope.

**Mirrors are GENERATED — never hand-create or hand-edit.** Run `bash scripts/sync-plugin-package.sh --check`
(read-only) first, then `bash scripts/sync-plugin-package.sh`. `.claude/skills/go/` is a real dir of
**per-file symlinks**; `.agents/skills/go` is **one directory symlink**; `plugins/gobbi/skills` is one
**top-level** symlink for the whole tree. **Writing through a mirror path converts a symlink to a regular
file and destroys the topology.**

**V8:** `ls -l .claude/skills/go/` → **18 entries, all symlinks**, none extra, **no `messaging.md` link**.
**V9 cold load — one runtime plus a WRITTEN waiver.** A **fresh agent** writes one `cold-load-result` for
`runtime: claude-code` with `selected_skill_type: operation`, `selected_type_child: operation-skill.md`,
`loaded_type_children: [operation-skill.md]`, `no_extra_type_child_proof.loaded_child_count: 1`.
**The codex record is a SEPARATE written block**: `waived-not-run`, system `codex`, step P7 cold load,
authority **D-10**, trigger *three consecutive upstream HTTP 503s on 2026-07-25*.
**A missing codex record without that written entry is a FAIL, not an omission.**
**NOTE:** you are Codex. If Codex is reachable in your session, **run the real cold load instead of the
waiver** and record it — D-10 exists only because Codex was unreachable.

---

## 4. State — what is done

| Task | State | Commit |
|---|---|---|
| 01 O-9 fetch ledger | ✅ 11 corrections | — (research) |
| 02 `SKILL.md` | ✅ | `03e3c217` |
| 03 modules-tooling / errors / interop | ✅ | `8df698bc` |
| 04 testing / performance / convention | ✅ | `84fe485b`, `2d702470` |
| 05 design / concurrency | ✅ | `3645a9ae` |
| 06 `service-clients.md` | ✅ | `e2375c0a`, `1c70bead` |
| 07 docker / kubernetes / aws | ✅ | `b42c2903` |
| 08 observability / grpc | ✅ | `31181cc5` |
| 09 triad | ✅ | `591dd4af` |
| 10 tree sweeps | ✅ all 10 green | `fe0c790d` |
| **11 wiring** | **NOT STARTED** | — |

**Nine `**Unverified:**` markers survive**, each naming what would resolve it. That is by design (O-9g):
an unsourceable claim ships with an explicit marker, never a fabricated citation.

---

## 5. Standing rules that produced this skill — keep them

1. **Citation is the only fact-check.** D-2 (no compile verification, Go toolchain not installed) and
   D-3 (Codex waived) mean no compiler and no second system ever ran. Every taught fact needs a primary
   owner.
2. **Before citing an owner, confirm that owner contains the claim.** Two High findings this session were
   citations that existed and did not support their claims.
3. **A rendered-page summary is a LEAD, not a citation.** A fetch of `pkg.go.dev/net/http/pprof` returned
   a confident *"Security Warning: never expose on a public or internet-facing interface"* — **that text
   does not exist in the source.** Read source/raw docs at a pinned tag.
4. **Pin the tag.** golangci-lint `master` serves v1-era content at **HTTP 200**; Prometheus' `main`
   CHANGELOG is a full minor stale; Go stdlib read at `master` is ahead of the released tag and produced
   a false finding that shipped into a child doc before being retracted.
   **A default-branch path and a 200 response are both assumptions.**
5. **A claim being true, useful and universally repeated is not evidence that it is OWNED.** Four such
   claims were confirmed unowned (D-23): string↔`[]byte` cost, struct field reordering, "reflection is
   slow", slice preallocation. Each is the skill's own claim, never attributed.
6. **Thinness is honesty; padding is the failure.** The delta test produced 41/51/43-line tool children
   against 70/200/120 estimates by omitting every hazard class with no genuine delta.
7. **A child adds no policy the parent lacks and never softens a parent categorical.**

---

## 6. The recurring defect, stated for the next manager

Every layer of this session verified the thing **one level shallower than the defect**:

| Round | Checked | Missed |
|---|---|---|
| Ideation 1 | the loudest risk (SDK rot) | the language core |
| Ideation 2 | the ledger's named items | the method that found them |
| Ideation 3 | file specifications literally | pointer-style specs |
| Ideation 4 | items | the clause *sites* an item occupies |
| Task 02 | that citations exist | whether they support |
| Task 10 | that sweeps run green | whether they can fail |

**And six errors traced to MANAGER BRIEFS**, each caught by an executor: an unstated resolved gate; a
stale fact contradicting the manager's own retraction; "resolved essentially all" when it was 13 of 22;
an unassigned page attribution; a re-contracted deleted item; and a fabricated quotation propagated from
research → artifact → brief → file.
**Root cause: briefs written from compressed recollection instead of re-reading the artifact.**
**A task brief is a citation surface too.**

---

## 7. Git

Publication is **`pull-request`** (D-1). Nothing pushed yet. After task 11 and Wrap-up PASS:
push the branch, open the PR against `develop`. **Merge stays a separate user decision** — do not merge.

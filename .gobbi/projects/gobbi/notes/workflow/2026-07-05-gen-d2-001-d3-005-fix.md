---
name: gen-d2-001-d3-005-fix
description: "GEN-D2-001 + GEN-D3-005 fix — read-project-rules contract aligned across 18 files on NO_PROJECT_RULES"
type: notes
scope: project
feature: null
status: active
created: 2026-07-05
session: 0bf4a2a4-9676-4159-a866-49d8e6fe3680
tags: [docs-sync, verification]
keywords: [rules-empty-state, NO_PROJECT_RULES, review-fix-campaign, GEN-D2-001, GEN-D3-005]
author: claude
features_touched: []
---

# GEN-D2-001 + GEN-D3-005 fix — read-project-rules contract alignment

## What happened

The 2026-07-01 adversarial-review FIX campaign continued with GEN-D2-001 (the "read project
rules" contract was stated inconsistently across the codebase) and GEN-D3-005 (a related
consistency defect in the same contract). The fix aligned the read-project-rules contract
across 18 files and centralized it on a single `NO_PROJECT_RULES` sentinel + empty-state
contract in `memory/rules.md`.

## What shipped

- **Central contract** — `memory/rules.md` § Empty-state contract for the `rules/` tier now
  defines the two-state read contract (`RULES_PRESENT` / `NO_PROJECT_RULES`) in ONE place; the
  16 aligned read-sites (5 role prompts, 4 delegation templates + `delegation/SKILL.md`, 5
  phase-doc read/record sites) plus the `memory-map.md` tier row all reference it.
- **Approach (a)** was chosen: one central contract every read-site points at, rather than
  repeating a fallback clause in each brief.
- **18 files** touched. Dual-system evaluation PASS.
- Branch `claude-2026-07-05-0bf4a2a4-9676-4159-a866-49d8e6fe3680`.
- Fix commits (5): `fc8091a2` (central contract + tier note), `4eb2da77` (role prompts),
  `bfe2292f` (delegation templates), `ece701ef` (phase-doc read/record sites), `eee58b87`
  (delegation mistake annotation). Wrap-up added `42abdf3a` (2 Low nits) + the
  memory-promotion commit.

## What got stuck

Nothing blocked. One escalation resolved mid-session: the Ideation-locked G1 wording used
guard-failing bare backtick paths, so the manager auto-applied the guard-safe rendering (the
locked DECISION was to annotate, not the exact path forms).

## What shifted

Three learnings promoted to memory:

1. Skill-owned trap **Skill-Surface Wording Must Pass Its Own Guard** →
   `skills/evaluation/mistakes.md`: an evaluator must RUN a planned guard against candidate
   wording, not just confirm the guard is listed in the plan.
2. Reinforced `mistakes/verification/manager-locked-decision-without-audit-trail-sync.md` with the
   new "prepend a superseding AMENDMENT block instead of reconciling the body" trigger.
3. New backlog `backlogs/workflow/integration-log-schema-drift.md` — production.md documents a
   4-column Integration Log; the validator requires a 5-column shape.

## Decisions to respect

- The read-project-rules contract lives in ONE place (`memory/rules.md` § Empty-state contract);
  do NOT re-derive a per-brief fallback — point every read-site at the central contract.
- `NO_PROJECT_RULES` is the sentinel for an absent/empty `rules/` tier; the project `rules/` tier is
  legitimately absent (it is lazily created — no rule has been promoted yet).

## Next session

Continue the 2026-07-01 review FIX campaign. This fix closes 2 remaining Highs (GEN-D2-001) and 1
Medium (GEN-D3-005). Remaining 2026-07-01 Highs: GEN-D1-002, GEN-D3-001, GEN-D3-002, GEN-D4-003.
Also open: GEN-D7-004 (Medium) that GEN-D1-003 depends on, plus the older G2 / G3 clusters. The
review FIX-campaign tracker note carries the full remaining set.

# Risk Perspective — Batch 4 iter2 (Claude)

## Stage 0 — Fix verification

- **Fix 4 (worktree remove --force in Forbidden Operations)** — git/SKILL.md line 121 adds `git worktree remove --force <path>` and `git worktree remove -f <path>` to the Forbidden table with Why ("silently discards any uncommitted changes") and Use Instead ("standard git worktree remove with clean tree check; escalate via AskUserQuestion if unclean"). Cross-cited at line 198 (Procedure P5) and line 236 (Operational notes). **Directly closes iter1 R-R-03 (partial).**
- **Fix 10 (sanitization annotation)** — gobbi/SKILL.md line 72 inserts a blockquote at the first `{project-name}` interpolation site, naming `packages/cli/src/lib/config/settings-io.ts` as the validation seam. **Directly closes iter1 R-R-02.**

## Inheritance from iter1

iter1 Risk verdict was PASS with 4 findings (1 Medium, 3 Low):

| Finding | iter1 severity | iter2 disposition |
|---|---|---|
| R-R-01 Session-start skills lack a high-leverage risk tag | Low | **Persists** — iter2 did not target. Re-asserted at Low. The Glossary partially compensates by naming "Sole-writer" as a constraint. |
| R-R-02 Project name interpolation not sanitized | Medium | **Addressed** by Fix 10. Sanitization annotation at line 72 names the validation seam and explicit fallback if bypassed. |
| R-R-03 Forbidden Operations omits destructive ops | Low | **Partially addressed** by Fix 4 (adds worktree remove --force/-f). Other potential omissions (git push --force, git reset --hard) were already covered in iter1's analysis — those remain. Re-asserted at Low. |
| R-R-04 Branch exclusivity recovery via P6 assumes recoverable orphan | Low | **Persists** — iter2 did not target. Re-asserted at Low. |

## New findings (iter2-introduced)

None. Fix 4's row is well-formed; Fix 10's sanitization blockquote names the exact seam file path and version-pinned validator location.

## Typed findings (iter2)

### R-R-01 (carryover) — Session-start skills lack a "high-leverage" risk callout

- **Type**: risk_blindspot
- **Domain**: process
- **Disposition**: open (persisted from iter1)
- **Confidence**: 45
- **Severity**: Low
- **Evidence**: gobbi/SKILL.md is loaded at session start by every agent and shapes every subsequent action. A typo or wrong instruction here has the largest blast radius of any skill. No explicit "high-leverage skill — changes require Always-Ask + extra evaluator coverage" tag in the frontmatter or top-of-skill.
- **Remediation**: Add a one-line "Risk class: high-leverage (session-start; affects every agent)" callout under the load-when paragraph.

### R-R-03 (carryover, downgraded) — Other destructive ops still uncovered

- **Type**: risk_blindspot
- **Domain**: process
- **Disposition**: open (persisted from iter1, partially addressed by Fix 4)
- **Confidence**: 50
- **Severity**: Low
- **Evidence**: Fix 4 adds `git worktree remove --force/-f` but other commonly-dangerous ops are still implicit: `git reset --hard` (Forbidden table mentions it via P6 escalation but no explicit row), `git push --force-with-lease` vs `--force` (only `--force` is explicitly Forbidden in the table), `git rebase --interactive` (not in table). For a solo-user system, the practical risk is bounded; for completeness it remains a gap.
- **Remediation**: Either add explicit rows for `git reset --hard <ref>` and `git push --force` (the latter is already there per the table — verify it's complete), or add a footnote that the table covers worktree-removal hazards and other classes are documented elsewhere.

### R-R-04 (carryover) — P6 branch-exclusivity recovery assumes recoverable orphan

- **Type**: risk_blindspot
- **Domain**: process
- **Disposition**: open (persisted from iter1)
- **Confidence**: 40
- **Severity**: Low
- **Evidence**: git/SKILL.md P6 (orphan recovery) assumes the orphaned branch is in a recoverable state (commits in reflog, branch ref not yet GC'd). If the orphan has been pruned, P6 silently fails. Iter1 raised this; iter2 did not target.
- **Remediation**: Add a precondition check to P6: "Verify `git reflog show <branch>` returns commits before proceeding; if empty, escalate."

## Low-confidence appendix

- **L-R-01 (confidence 30)** — Fix 10's sanitization blockquote covers `{project-name}` but the skill uses other slot values too (`{session-id}`, `{date}`, `{task-id}`). The blockquote implies "and similar slot values" — would benefit from naming each one explicitly. Low confidence — `session-id` and `date` are generated by gobbi (low forgery risk) and `task-id` is also gobbi-generated.
- **L-R-02 (confidence 25)** — Fix 4's row mentions "force-remove silently discards any uncommitted work inside the worktree (Forbidden Operations)." The parenthetical self-reference (the Forbidden Operations table referencing itself) is slightly recursive. Minor.

## Verdict

**PASS** — Fix 4 cleanly closes the worktree-remove-force gap; Fix 10 cleanly closes the sanitization annotation gap. Three iter1 Lows persist as carryovers, none a blocker. No new risk findings. Risk converges PASS in iter2.

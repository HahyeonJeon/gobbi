## Findings

**Type:** design_flaw
**Severity:** High
**Confidence:** 95
**Evidence:** Promotion coverage and routing are sound: the staging inventory and manifest both enumerate 32 files, every inventory source has a manifest row, `all_promote_destinations_exist`, and the routing-table check returned `routing_ok`. The five required process mistakes are present under `.gobbi/projects/gobbi/mistakes/`, have `scope: project`, and each includes the required what/why/how-to-recognize/corrected sections. The journal and handoff exist, and spot-checked pointers resolve. However, the promoted project-memory files violate the wrap-up/memorization frontmatter allowlist and make the handoff's grep-gate claim false. Running the §4.5 live-memory grep gate printed 23 files under `.gobbi/projects/gobbi/features/project-memory/` and `.gobbi/projects/gobbi/backlogs/`; field counts included `finding-id` 13, `confidence` 11, `severity` 11, `loop` 9, `iter` 6, `scenario` 2, and `task` 2. Examples: `features/project-memory/scenarios/tier-2-3-scope-explicitly-placed.md:12` has `finding-id: codex-f3`; `features/project-memory/decisions/codex-path-traceability.md:10-11` has `severity` and `confidence`; `features/project-memory/plans/2026-05-26-dev-doc-standard-retrofit.md:11` has `task:`. The separate conditional disposition check found 18 non-backlog files with `disposition:`. This contradicts `wrap-up/artifacts/handoff.md`, which claims the grep gate has 0 hits after the conformance wave, and it means Wrap-up promoted staging/evaluation metadata into durable memory instead of applying the type-aware allowlist.
**Fix:** Re-run the promotion write step for the promoted files through the destination-type allowlist in `skills/memorization/rules.md` §2.1-§2.3 and `skills/wrap-up/SKILL.md` "Frontmatter allowlist on promotion". Strip staging/evaluation keys from non-session project memory (`finding-id`, `confidence`, `severity`, `loop`, `iter`, `scenario`, `task`, and non-backlog `disposition`), preserving legitimate per-type extension keys. Then rerun both §4.5 gates: the main S-set grep must print zero files, and the conditional `^disposition:` check excluding `*/backlogs/*` must print zero files. Update the handoff only after those commands are clean.

VERDICT: REVISE

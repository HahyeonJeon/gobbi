# Usage — T7 conform install-runtime-b (commit 6f9dbf9)

## Artifact Summary + Memory reads
See project.md. Consumer = future agent/user opening these docs cold (the §4.1 zero-context reader).
Memory reads: as project.md; memorization/rules.md §4.1 zero-context-reader definition.

## Locked Frame (Stage 1)
**Zero-context reader understands each doc without the originating session (§4.1)**
- [ ] No load-bearing session coordinate remains in any body (§4.3)
- [ ] Technical knowledge (facts, commands, numbers) survives the de-crypt
- [ ] `## Source` footers point to a resolvable canonical artifact, not a vanished iter path
**Reader arriving cold at a backlog knows why it is deferred and when to pick up (adversarial: de-crypt strips the actionable why)**
- [ ] Each backlog retains Context + Why-deferred + When-to-pick-up self-contained

## Per-scenario per-check results
- no load-bearing coords in bodies: YES — residual scan (iter<n>/COD-/CL-/CP-/F-Fix/RATIFY-/draft-iter/row-<n>/FIX-<n>) over all 20 bodies = 0; broader scan (T<n>/D-<n>/Task 0<n>/Sub-step) = 0 except the one pre-existing README "Recent activity" `W3-T0` row (see aesthetics A1).
- technical knowledge survived: YES — e.g. dot-gobbi backlog keeps the empirical "ls returns No such file" fact, the two resolver steps, the {"name":"gobbi"} payload; changelog ref keeps v2.1.132 + "7 references corrected"; mirror-policy checklist keeps all 4 verification rows + commands.
- Source footers resolvable: YES — footers now read "Surfaced during install-runtime preparation/design evaluation (session 1b26cf20)" instead of `evaluation/iter3/codex/...` dead paths; the originating-session frontmatter + (where present) `## Originating session` give the canonical pointer.
- backlogs self-contained: YES — each of the 7 retains Context/Why-deferred/Suggested-approach/When-to-pick-up.

## Typed findings
- See aesthetics A1 (README W3-T0 residual) — shared evidence, recorded once there.

## Low-confidence appendix
None.

VERDICT: PASS

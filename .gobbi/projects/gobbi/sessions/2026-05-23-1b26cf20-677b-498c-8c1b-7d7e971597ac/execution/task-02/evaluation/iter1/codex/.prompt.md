# Codex Evaluator — Task 02 iter1

## Identity guard

CCSI=1b26cf20-677b-498c-8c1b-7d7e971597ac

## Step 0 — marker discipline

Before any evaluation:

1. Verify `.wrapper-marker` exists at the absolute path below. If missing, STOP and emit BLOCKED.
2. Write `.codex-marker` at the same absolute directory with ISO-8601 timestamp content.

Absolute output dir (CANONICAL — all writes use this path):
`/playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-02/evaluation/iter1/codex/`

```bash
# Step 0a — verify wrapper-marker
test -f /playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-02/evaluation/iter1/codex/.wrapper-marker || { echo "BLOCKED: wrapper-marker missing"; exit 1; }

# Step 0b — write codex-marker
date -u +"%Y-%m-%dT%H:%M:%SZ" > /playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-02/evaluation/iter1/codex/.codex-marker
echo "codex-marker written"
```

---

## Evaluation Target

Commit: `97ae373` on worktree `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/worktrees/chore/268-session-foundations-bundle-b`

Primary command:
```bash
git -C /playinganalytics/git/gobbi/.gobbi/projects/gobbi/worktrees/chore/268-session-foundations-bundle-b show 97ae373
```

This commit implements T1-I-T1.b and T1-I-T1.c (Tasks 02 of session-foundations-bundle-b):
- **T1.b**: Qualifies `git/SKILL.md` Memory Access Matrix row "Session notes / mistakes" and the Critical rule to use `session.json.git.worktreePath` as the absolute root when set; fallback to main tree when null (direct mode). Adds transcript path note.
- **T1.c**: Adds one-sentence P2 invocation note — P2 is invoked from Configuration row 5.5 for worktree-first sessions (orchestration/SKILL.md Step 1), not from Execution start.

Changed file: `.gobbi/projects/gobbi/skills/git/SKILL.md` — 4 insertions, 2 deletions.

## Plan verification criteria (from plan.md)

```yaml
id: 02-git-skill-worktree-path-qualifier
verifies:
  - grep -E 'worktreePath' .claude/skills/git/SKILL.md returns ≥2 matches
  - test -L .claude/skills/git/SKILL.md
```

Note: the symlink `.claude/skills/git/SKILL.md` points to `.gobbi/projects/gobbi/skills/git/SKILL.md`. The actual change lives in the worktree at `.gobbi/projects/gobbi/skills/git/SKILL.md`. Verify `worktreePath` appearance count in the worktree copy.

## Load Directives

1. Load principles skill: `/playinganalytics/git/gobbi/.claude/skills/principles/SKILL.md`
2. Load rules from: `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/rules/`
3. Load mistake skill: `/playinganalytics/git/gobbi/.claude/skills/mistake/SKILL.md`
4. Load codex skill: `/playinganalytics/git/gobbi/.claude/skills/codex/SKILL.md`
5. Load evaluation skill: `/playinganalytics/git/gobbi/.claude/skills/evaluation/SKILL.md`
6. Load execution evaluation child doc: `/playinganalytics/git/gobbi/.claude/skills/execution/evaluation.md`

## Applicable mistakes to check before evaluating

- `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/mistakes/codex-rescue-agent-fire-and-forget-without-result-capture.md`
- `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/mistakes/codex-eval-session-write-path-nested-in-worktree.md`
- `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/mistakes/evaluator-returned-verdict-inline-no-per-perspective-files.md`

---

## 4-Stage Evaluation

This is iter 1. No prior iter to inherit from.

Workflow phase: `execution`

### Stage 0 — Target Understanding

Read the full commit diff. The artifact is a documentation change to a markdown skill file (`git/SKILL.md`). This is a text-only artifact; tool verification via grep/file-existence is the strongest method.

Extract What / Why / How:
- **What**: Modified Memory Access Matrix row + Critical rule paragraph + P2 procedure preamble in `git/SKILL.md`
- **Why**: T1.b — qualify session-write root to use worktreePath when set (no longer unconditionally main tree); T1.c — clarify P2 is invoked from Configuration row 5.5, not Execution start
- **How**: In-place surgical edit of 2 rows; 1 P2 paragraph insertion

Load the execution evaluation child doc at `/playinganalytics/git/gobbi/.claude/skills/execution/evaluation.md` for seed scenarios.

Check plan verification criteria:
```bash
# Check worktreePath count in the worktree's version of the file
git -C /playinganalytics/git/gobbi/.gobbi/projects/gobbi/worktrees/chore/268-session-foundations-bundle-b \
  show 97ae373:.gobbi/projects/gobbi/skills/git/SKILL.md | grep -c 'worktreePath'

# Check symlink
test -L /playinganalytics/git/gobbi/.claude/skills/git/SKILL.md && echo "symlink OK" || echo "WARN: not a symlink"
```

### Stage 1 — Scenario-Checklist Frame Build (per perspective)

Build locked Frame for each perspective. Apply adversarial scenario requirement. For a text-only documentation change, typical adversarial scenarios are: contradictions with other docs, incomplete fallback logic, ambiguous new terminology, and side effects on consumers who rely on old wording.

### Stage 2 — Per-Perspective Sequential Evaluation

7 perspectives in order: Project → Structure → Performance → Aesthetics → Usage → Consistency → Risk

For each perspective:
1. Walk the locked Frame
2. Record yes/no per checklist item with evidence (quotes from the diff or grep results)
3. Note any unanticipated finding with Type+Domain+Confidence+Severity
4. Compute per-perspective verdict

**Specific checks required by the evaluation brief (surfaced-by: codex)**:

1. **Memory Access Matrix qualifier completeness**: Does the new row text clearly specify the fallback from `worktreePath` to main-tree? Is the condition for "when set" vs "null (direct mode)" unambiguous?

2. **P2 invocation note accuracy**: Does the added paragraph at P2 accurately state that P2 is invoked from Configuration row 5.5, and that executors are passed the existing `session.json.git.worktreePath`? Is there any internal contradiction with P2's procedural body?

3. **Symlink chain integrity**: The file is `.gobbi/projects/gobbi/skills/git/SKILL.md` in the worktree. The symlink `.claude/skills/git/SKILL.md → .gobbi/projects/gobbi/skills/git/SKILL.md` must be intact. Verify.

4. **worktreePath count**: Verify `grep -c 'worktreePath'` in the committed file returns ≥ 2. This is the plan's verification criterion.

5. **Output paths section consistency**: The Output paths section at the bottom of `git/SKILL.md` still says "never the worktree path" for session notes/mistakes. This may now contradict the updated Memory Access Matrix (which says USE worktreePath when set). Check whether this section needs a corresponding update.

6. **Constraints section consistency**: `git/SKILL.md` Constraints section has: "MUST write notes and mistakes to the main tree absolute path — never the worktree path." This is the OLD rule. Does the commit update this constraint line? If not, is this an inconsistency?

7. **Transcript path note**: The new matrix row adds "Transcript path (`session.json.transcriptPath`) lives in user home (`~/.claude/projects/`) — not under either tree." Verify this is factually accurate and does not conflict with how transcripts are used elsewhere in the skill set.

### Stage 3 — Overall

After all 7 perspectives, produce holistic verdict. Check Karpathy's 4 failure modes. Identify preserve list.

---

## 5-Type Vocabulary Requirement

Every finding must use one of these types (surfaced-by: codex):
- `scenario_gap`
- `checklist_gap`
- `design_flaw`
- `assumption_risk`
- `general`

---

## Output Requirements

Write **8 files** using the absolute path below. DO NOT use relative paths. DO NOT construct paths from `pwd`. The worktree CWD is NOT the session-write root.

All writes MUST use this absolute base:
`/playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-02/evaluation/iter1/codex/`

Files to write:
1. `project.md` — Project perspective (Stage 1 Frame + Stage 2 findings + verdict)
2. `structure.md` — Structure perspective
3. `performance.md` — Performance perspective
4. `aesthetics.md` — Aesthetics perspective
5. `usage.md` — Usage perspective
6. `consistency.md` — Consistency perspective
7. `risk.md` — Risk perspective
8. `overall.md` — Stage 3 overall verdict + cross-cutting findings + Karpathy checks + Preserve list

Each file MUST start with:
```yaml
---
session: 1b26cf20-677b-498c-8c1b-7d7e971597ac
loop: execution
task: task-02
system: codex
iter: 1
perspective: <name>
verdict: <PASS|REVISE|FAIL>
---
```

And MUST contain:
- `## Artifact Summary` (Stage 0)
- `## Memory reads` subsection (all files read)
- `## Locked Frame (Stage 1)` (scenarios + attached checklists)
- `## Stage 2 Findings` (per-scenario per-check results + typed findings)
- `## Per-perspective verdict` line

`overall.md` additionally MUST contain:
- `VERDICT: <PASS|REVISE|FAIL>` on its own line

DO NOT return findings inline in your response. Write ALL findings to the 8 contracted output files.

## Write method

Use Bash with heredoc for each file:
```bash
mkdir -p /playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-02/evaluation/iter1/codex/
cat > /playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-02/evaluation/iter1/codex/project.md << 'ENDOFFILE'
... content ...
ENDOFFILE
```

## Commit trailer

The commit under evaluation MUST carry `AI-Provenance-Record:` trailer (not `Co-Authored-By:`). Verify this in the commit metadata.

## Final post-write verification

After writing all 8 files:
```bash
ls /playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-02/evaluation/iter1/codex/*.md | wc -l
# Must be 8

grep "^VERDICT:" /playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-02/evaluation/iter1/codex/overall.md
# Must produce a line

grep -l "worktreePath" /playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-02/evaluation/iter1/codex/*.md | wc -l
# Should be > 0 (verifies 5-Type vocabulary appears; worktreePath is the subject)
```

If any verification fails, fix and re-verify before reporting DONE.

## BLOCKED conditions

Report BLOCKED if:
- `.wrapper-marker` is missing from the output dir
- You cannot write to the absolute output dir
- The commit 97ae373 cannot be read from the worktree

Report DONE only after all 8 files exist at the contracted absolute path and all post-write checks pass.

## Artifact Summary

Task 03 evaluates commit `6f1df8c34369f9fed7e2f4e5ab309ef23ac2a759` in worktree `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/worktrees/chore/268-session-foundations-bundle-b`. What: extend `.gobbi/projects/gobbi/skills/preparation/SKILL.md` so the Preparation `generate-now` narrow exception includes a `git -C "$worktreePath"` add/commit path, the AI provenance trailer form, and rollback via `git -C "$worktreePath" rm` plus AskUserQuestion. Why: Planning Task 03 and LOCK #4 require the promote-now path to survive worktree sessions and to remove copied files on failed commit instead of using checkout. How: one prose insertion after the generated-skills exception, visible through the `.claude/skills/preparation/SKILL.md` symlink.

Memory reads: evaluation prompt at `execution/task-03/evaluation/iter1/codex/eval-prompt.md`; project rule `rules/stub-redirect-format.md`; mistakes `codex-rescue-agent-fire-and-forget-without-result-capture.md`, `codex-eval-session-write-path-nested-in-worktree.md`, and `evaluator-returned-verdict-inline-no-per-perspective-files.md`; execution evaluation child doc `.agents/skills/execution/evaluation.md`; planning task in `planning/artifacts/plan.md:78-94`; locked design `ideation/staging/design/d-3-promote-now-commit-on-branch.md`; planning decision `planning/staging/decisions/rollback-semantics-drift-from-ideation.md`; git trailer convention `.agents/skills/git/conventions.md:118`.

## Locked Frame (Stage 1)

Scenario: Implementation matches Planning Task 03.
- Check: the target file is the only modified file.
- Check: all verify grep gates pass.
- Check: rollback is co-located with the narrow exception.

Scenario: Implementation matches Ideation D-3.
- Check: command uses `git -C "$worktreePath" add <promoted-files>` or equivalent.
- Check: command uses `git -C "$worktreePath" commit` with subject `chore(skills): promote {slug} generated by preparation iter{n}`.
- Check: command includes the AI-Provenance-Record body/trailer mechanism from D-3.
- Check: rollback uses `git -C "$worktreePath" rm` and AskUserQuestion.

Scenario: The task passes regex gates but drifts from the richer locked design (adversarial).
- Check: grep gates are compared against D-3, not treated as the whole contract.
- Check: the trailer is checked as an executable commit-body instruction, not only as a visible string.

Scenario: Cross-artifact sync.
- Check: `.claude/skills/preparation/SKILL.md` resolves to the `.gobbi` source file.
- Check: `git/conventions.md:118` trailer form is cited and matched.

## Stage 2 Evaluation

Planning Task 03:
- yes: only `.gobbi/projects/gobbi/skills/preparation/SKILL.md` is modified.
- yes: all specified grep gates passed.
- yes: rollback is co-located at line 71.

Ideation D-3:
- yes: line 66 uses `git -C "$worktreePath" add <relative-paths>`.
- yes: line 67 uses the required `chore(skills): promote {slug} generated by preparation iter{n}` subject.
- no: D-3 lines 18-21 show a two-`-m` commit command, with the second `-m` writing the `AI-Provenance-Record` body. The implemented skill has the trailer string only after an em dash-style explanatory clause, not in the command.
- yes: line 71 uses `git -C "$worktreePath" rm` and AskUserQuestion.

Adversarial regex-vs-design scan:
- no: `grep -E 'gobbi://session/'` passes, but `grep -n -- '-m "AI-Provenance-Record:' "$WORKTREE/.claude/skills/preparation/SKILL.md"` returns no match. The regex gate proves the string exists, not that the commit command writes the trailer.

Cross-artifact sync:
- yes: `.claude/skills/preparation/SKILL.md` is a symlink to `../../../.gobbi/projects/gobbi/skills/preparation/SKILL.md`.
- yes: the visible trailer form matches `git/conventions.md:118`.

## Findings

Finding C1: implementation drifts from D-3's two-`-m` commit command.
- Type: `design_flaw`
- Domain: `docs-sync`
- Confidence: 100
- Severity: High
- Evidence: locked design `d-3-promote-now-commit-on-branch.md:18-21` specifies `git -C "$worktreePath" commit \` with both the subject `-m` and a second `-m "AI-Provenance-Record: ..."` body argument. Implemented `preparation/SKILL.md:67` includes only the subject `-m`; grep for `-m "AI-Provenance-Record:` in the implemented symlink target returned no output.
- Disposition: open

## Verdict: REVISE

The change satisfies the task's coarse grep gates but does not preserve the richer locked D-3 command shape.

## Low-confidence appendix

None.

## Artifact Summary

Task 03 evaluates commit `6f1df8c34369f9fed7e2f4e5ab309ef23ac2a759` in worktree `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/worktrees/chore/268-session-foundations-bundle-b`. What: extend `.gobbi/projects/gobbi/skills/preparation/SKILL.md` so the Preparation `generate-now` narrow exception includes a `git -C "$worktreePath"` add/commit path, the AI provenance trailer form, and rollback via `git -C "$worktreePath" rm` plus AskUserQuestion. Why: Planning Task 03 and LOCK #4 require the promote-now path to survive worktree sessions and to remove copied files on failed commit instead of using checkout. How: one prose insertion after the generated-skills exception, visible through the `.claude/skills/preparation/SKILL.md` symlink.

Memory reads: evaluation prompt at `execution/task-03/evaluation/iter1/codex/eval-prompt.md`; project rule `rules/stub-redirect-format.md`; mistakes `codex-rescue-agent-fire-and-forget-without-result-capture.md`, `codex-eval-session-write-path-nested-in-worktree.md`, and `evaluator-returned-verdict-inline-no-per-perspective-files.md`; execution evaluation child doc `.agents/skills/execution/evaluation.md`; planning task in `planning/artifacts/plan.md:78-94`; locked design `ideation/staging/design/d-3-promote-now-commit-on-branch.md`; planning decision `planning/staging/decisions/rollback-semantics-drift-from-ideation.md`; git trailer convention `.agents/skills/git/conventions.md:118`.

## Locked Frame (Stage 1)

Scenario: A future manager can execute the documented commit path without reverse-engineering.
- Check: the add command identifies what should be staged.
- Check: the commit command includes a concrete subject.
- Check: the trailer form is present in a way the manager can operationalize into the commit body.

Scenario: A future manager can recover from commit failure.
- Check: rollback names `git -C "$worktreePath" rm <copied-paths>`.
- Check: rollback names AskUserQuestion and the re-attempt-or-abort decision.
- Check: rollback explicitly rejects `git checkout`.

Scenario: The copy-paste command path silently drops the provenance trailer (adversarial).
- Check: the command form includes either a second `-m "AI-Provenance-Record: ..."` argument or an equally concrete `--trailer`/body mechanism.
- Check: the prose does not rely on an implied extra command the consumer must invent.

Scenario: Accessibility and i18n coverage.
- not-applicable: this is agent-facing Markdown, not UI. Headings and short lists make the procedure scan-friendly; no user-facing localizable strings or locale-sensitive logic are introduced.

## Stage 2 Evaluation

Executable commit path:
- yes: line 66 states `git -C "$worktreePath" add <relative-paths>` and says it stages the skill body and both symlinks.
- partial: line 67 gives a concrete commit subject and includes the trailer string in prose.
- no: line 67's command itself has only one `-m` argument, while the locked D-3 command has a second `-m "AI-Provenance-Record: ..."` body argument at `d-3-promote-now-commit-on-branch.md:21`.

Rollback path:
- yes: line 71 requires `git -C "$worktreePath" rm <copied-paths>`.
- yes: line 71 names AskUserQuestion and "re-attempt or abort".
- yes: line 71 explicitly says not `git checkout`.

Adversarial trailer-loss path:
- no: `grep -n -- '-m "AI-Provenance-Record:' "$WORKTREE/.claude/skills/preparation/SKILL.md"` produced no output, while the locked design includes that exact second `-m` at line 21.
- yes: the trailer string matches `git/conventions.md:118`, but the command shape is not operationally complete.

## Findings

Finding U1: commit command can be copied without writing the trailer body.
- Type: `design_flaw`
- Domain: `docs-sync`
- Confidence: 100
- Severity: High
- Evidence: `preparation/SKILL.md:67` documents `git -C "$worktreePath" commit -m "chore(skills): promote {slug} generated by preparation iter{n}"` and then describes the trailer in prose. The locked D-3 design shows the operational command with a second `-m "AI-Provenance-Record: gobbi://session/{session-id}/task/preparation-promote-now-iter{n}"` at `d-3-promote-now-commit-on-branch.md:21`; grep for that second `-m` in the implemented skill returned no matches.
- Disposition: open

## Verdict: REVISE

The next consumer can satisfy the visible subject command while accidentally omitting the provenance trailer from the commit body.

## Low-confidence appendix

None.

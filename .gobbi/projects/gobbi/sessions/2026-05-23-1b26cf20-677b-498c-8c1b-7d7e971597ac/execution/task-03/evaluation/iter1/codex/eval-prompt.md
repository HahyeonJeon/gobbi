# Codex Evaluator — Task 03 iter1 Execution Evaluation

## Session Guard

CCSI=1b26cf20-677b-498c-8c1b-7d7e971597ac

FIRST action: write a `.codex-marker` file to confirm you are running in the correct session:

```bash
cat > /playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-03/evaluation/iter1/codex/.codex-marker << 'EOF'
codex-agent=active
session-id=1b26cf20-677b-498c-8c1b-7d7e971597ac
task=03
iter=1
system=codex
EOF
```

ABORT if you cannot write this file.

## Your Role

You are the Codex-side evaluator for Task 03 iter1 of session `2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac`. You evaluate the commit `6f1df8c` on worktree `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/worktrees/chore/268-session-foundations-bundle-b` against Task 03's spec.

## Load Directives

1. Read `/playinganalytics/git/gobbi/.claude/skills/principles/SKILL.md`
2. Read `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/rules/stub-redirect-format.md`
3. Read `/playinganalytics/git/gobbi/.claude/skills/mistake/SKILL.md`
4. Read `/playinganalytics/git/gobbi/.claude/skills/evaluation/SKILL.md`
5. Read mistakes:
   - `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/mistakes/codex-rescue-agent-fire-and-forget-without-result-capture.md`
   - `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/mistakes/codex-eval-session-write-path-nested-in-worktree.md`
   - `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/mistakes/evaluator-returned-verdict-inline-no-per-perspective-files.md`

## Artifact

Run this command to read the full commit diff (this is your PRIMARY artifact):

```bash
git -C /playinganalytics/git/gobbi/.gobbi/projects/gobbi/worktrees/chore/268-session-foundations-bundle-b show 6f1df8c
```

The commit modifies `.gobbi/projects/gobbi/skills/preparation/SKILL.md` (9 lines added, adding two new prose sections after the generate-now paragraph).

## Task 03 Spec (for evaluation scope — this is the scope contract)

```yaml
id: 03-preparation-generate-now-commit-on-branch
what: Extend preparation/SKILL.md narrow-exception with git -C "$worktreePath" add + commit promote-now path + AI-Provenance-Record trailer. Per LOCK #4, rollback semantics co-located: if git commit fails post-copy, manager MUST git -C "$worktreePath" rm <copied-paths> + AskUserQuestion + re-attempt-or-abort. Rollback REMOVES the copied file (not git checkout). Per Ideation:283.
requires: [01-orchestration-row-5-5-worktree-create, 02-git-skill-worktree-path-qualifier]
files:
  - {path: ".gobbi/projects/gobbi/skills/preparation/SKILL.md", op: modify}
verifies:
  - grep -E 'git -C "\$worktreePath"' .claude/skills/preparation/SKILL.md returns ≥3 matches
  - grep -E 'chore.skills.: promote' .claude/skills/preparation/SKILL.md returns ≥1 match
  - grep -E 'gobbi://session/' .claude/skills/preparation/SKILL.md returns ≥1 match
  - grep -E 'git -C "\$worktreePath" rm' .claude/skills/preparation/SKILL.md returns ≥1 match
  - grep -E 'AskUserQuestion' .claude/skills/preparation/SKILL.md returns ≥1 match co-located with rollback
  - test -L .claude/skills/preparation/SKILL.md
effort: Medium
```

## LOCK #4 Decision (critical context for evaluation)

Rollback semantics live co-located with the narrow-exception text in preparation/SKILL.md (NOT git/SKILL.md).
Rollback sequence per Ideation:283:
1. `git commit` fails post-copy
2. Manager: `git -C "$worktreePath" rm <copied-paths>` — removes the copied skill file
3. AskUserQuestion: surface failure to user
4. Re-attempt or abort per user response

**Critical**: rollback REMOVES (not `git checkout`). The file did not pre-exist in the worktree.

## What to Verify (Specific Checks)

For `git -C "$worktreePath" add` and `git -C "$worktreePath" commit` steps, verify:
- The add step stages skill body and symlinks
- The commit step includes the AI-Provenance-Record trailer form: `AI-Provenance-Record: gobbi://session/{session-id}/task/preparation-promote-now-iter{n}`
- The commit subject form: `chore(skills): promote {slug} generated by preparation iter{n}`
- The trailer matches the form in `git/conventions.md` line 118

For rollback:
- `git -C "$worktreePath" rm` used (NOT `git checkout`)
- AskUserQuestion cited in rollback prose
- Ideation:283 cited or behavior consistent with it

For symlinks:
- Verify that `.claude/skills/preparation/SKILL.md` is a symlink to the `.gobbi` counterpart

Run these verifications in the worktree:

```bash
WORKTREE=/playinganalytics/git/gobbi/.gobbi/projects/gobbi/worktrees/chore/268-session-foundations-bundle-b
git -C "$WORKTREE" show 6f1df8c -- .gobbi/projects/gobbi/skills/preparation/SKILL.md | grep -c 'worktreePath'
grep -E 'git -C "\$worktreePath"' "$WORKTREE/.claude/skills/preparation/SKILL.md" | wc -l
grep -E 'chore.skills..* promote' "$WORKTREE/.claude/skills/preparation/SKILL.md"
grep -E 'gobbi://session/' "$WORKTREE/.claude/skills/preparation/SKILL.md"
grep -E 'git -C "\$worktreePath" rm' "$WORKTREE/.claude/skills/preparation/SKILL.md"
grep -E 'AskUserQuestion' "$WORKTREE/.claude/skills/preparation/SKILL.md"
test -L "$WORKTREE/.claude/skills/preparation/SKILL.md" && echo "symlink: yes" || echo "symlink: no"
```

## 5-Type Finding Vocabulary (REQUIRED)

Every finding in every perspective file MUST use one of these Type values:
- `scenario_gap`
- `checklist_gap`
- `design_flaw`
- `assumption_risk`
- `general`

## Evaluation Procedure

Run the full 4-stage evaluation per `evaluation/SKILL.md`:
- Stage 0: Target Understanding (extract W/W/H; load execution/evaluation.md child doc conceptually)
- Stage 1: Scenario-Checklist Frame Build (7 perspectives; include adversarial scenario for each)
- Stage 2: Per-Perspective Sequential Evaluation (Project → Structure → Performance → Aesthetics → Usage → Consistency → Risk)
- Stage 3: Overall

Iteration = 1 (no prior iter to inherit).

## Output Files

Write EXACTLY 8 files using Bash heredoc (`cat > path << 'EOF' ... EOF`). NEVER return findings inline in chat.

Output directory: `/playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-03/evaluation/iter1/codex/`

Files:
1. `project.md`
2. `structure.md`
3. `performance.md`
4. `aesthetics.md`
5. `usage.md`
6. `consistency.md`
7. `risk.md`
8. `overall.md`

Each per-perspective file MUST contain:
- `## Artifact Summary` (Stage 0)
- `## Locked Frame (Stage 1)` — scenarios with attached checklists; ≥1 adversarial scenario or explicit `not-applicable:` rationale
- `## Stage 2 Evaluation` — per-scenario per-check yes/no with evidence
- `## Findings` — each with: Type / Domain / Confidence / Severity / Evidence / Disposition (all iter1 default to `open`)
- `## Verdict: <PASS|REVISE|FAIL>`
- `## Low-confidence appendix`

`overall.md` MUST contain:
- `## Overall Findings`
- `## Karpathy failure modes check`
- `## Preserve list`
- `VERDICT: <PASS|REVISE|FAIL>` on its own line

## Critical Constraints

- All session writes MUST use the absolute main-tree path above. Do NOT use relative paths or `pwd`-derived paths. The worktree CWD is NOT the session-write root.
- Do NOT return findings inline. Write all 8 files via Bash heredoc.
- Verify: run `ls /playinganalytics/git/gobbi/.gobbi/projects/gobbi/sessions/2026-05-23-1b26cf20-677b-498c-8c1b-7d7e971597ac/execution/task-03/evaluation/iter1/codex/ | wc -l` after writing — must be ≥ 9 (marker + 8 eval files).
- Use the 5-Type vocab in EVERY finding's Type field.
- `overall.md` MUST have `VERDICT:` line at the top level.

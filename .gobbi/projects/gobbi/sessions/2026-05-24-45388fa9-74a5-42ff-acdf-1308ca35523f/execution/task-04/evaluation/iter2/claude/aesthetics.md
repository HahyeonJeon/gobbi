# Aesthetics — no regression

The new JSON example (P1) and the two fenced bash smoke-test blocks (P7) follow the existing
fenced-code style of the document; indentation and language tags consistent. Readability improved
(concrete payloads replace an opaque `...`). No new finding. PASS.

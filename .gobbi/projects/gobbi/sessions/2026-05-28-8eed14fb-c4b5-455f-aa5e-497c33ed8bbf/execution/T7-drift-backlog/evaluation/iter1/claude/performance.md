# T7 evaluation — performance perspective (iter1)

**Target:** `.gobbi/projects/gobbi/backlogs/model-assignment-drift-delegation-vs-settings-default.md`

**Perspective:** performance — efficiency for the future reader (the "performance" of the doc as an information artifact).

## Stage 0–1: Frame
- Performance for a memory doc = how quickly a zero-context reader extracts the actionable knowledge (§4.1 dev-doc bar).
- Key metric: can a future agent open the file and act without re-deriving session context?

## Stage 2
- Title states subject directly. Frontmatter `description` summarizes the conflict in one line.
- Body opens with the conflict in a 2-line bullet pair (lines 23-24) — fast diagnostic read.
- "Inversion is complete" (line 26) — high-signal single sentence summarizes the asymmetry.
- Three lettered resolution options (a/b/c) — pre-structured for picking-up session, removes re-derivation cost.
- Origin section short, points at finding ID + iter — provenance preserved without burying body.

## Findings
None. The doc is information-dense and front-loads the critical claim.

## Verdict
**PASS** — Reader can extract the conflict, its severity, and the resolution menu in under 60 seconds. Strong performance for a backlog item.

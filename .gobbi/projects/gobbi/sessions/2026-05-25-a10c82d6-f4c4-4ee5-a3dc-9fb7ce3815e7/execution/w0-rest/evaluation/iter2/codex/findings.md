# Codex Adversarial Evaluation - W0-rest iter2 remediation

Target: `2f86cb1` (`docs(memory-redesign): W0-rest iter2`) at current `HEAD`.
Method: re-read target docs/templates and reran the requested grep/diff/symlink gates from the worktree root. Producer and Claude-side PASS claims were not trusted.

[HIGH|design_flaw|95] COD-CONS-001 - NOT CLOSED. The requested positive evidence is present: `orchestration/SKILL.md` still documents `state.json` as the live workflow state-machine (`row 5.5` initialization, canonical session tree, session-root files, and `### State persistence`), and the design-of-record has dated `CORRECTION 2026-05-26 (user-ratified)` text at section 3.4 and section 7 #7. `grep -ci CORRECTION memory-system-redesign-design.md` returns `3`. However, section 8 still contains an active migration-plan instruction to "remove the **2** root `HANDOFF.md` + the **5** `state.json`" at line 488. That stale section 8 instruction is not struck through or corrected, so the design still tells Planning/Execution to remove the live state-machine files despite the user-ratified KEEP decision.

[LOW|general|100] COD-CONS-002 - CLOSED. `grep -c 'scope: project' .gobbi/projects/gobbi/skills/memorization/templates/changelogs.md` returns `0`; the invented project-level/per-session changelog variant is gone and the session roll-up is routed to `notes/`. `mistakes.md` now documents feature-scoped mistakes at `.gobbi/projects/{project-name}/features/{feature-name}/mistakes/` and explicitly says feature-specific traps do not live in project `mistakes/` with only a `feature:` tag. `references.md` now documents feature-level default routing to `features/{feature-name}/references/` with rare project-level promote-up. These match the concrete design section 2 targets and Wrap-up routing rows.

[LOW|general|95] COD-CONS-003 - CLOSED for the iter1 allowlist finding. The previously flagged fields are no longer frontmatter keys: `task`, `related`, `category`, `coverage`, `reviewer`, `perspectives`, `related_reports`, and `related_decisions` moved into body sections (`**Task:**`, `## Related`, `**Category:**`, `**Coverage:**`, `## Reviewer + scope`, and review `## Related`). Extracted remaining template frontmatter keys are base fields plus allowed/sanctioned type fields: `references` uses `title/source/accessed/ref_type`; `reviews` uses `review_kind/subject/verdict`; `scenarios` uses base only; `changelogs` keeps `shipped_in`, which is design section 2.14's feature-subdir changelog extension and was not one of the flagged ad-hoc fields.

[LOW|general|100] W0-GATE - CLOSED. `grep -c '## Principle 13' .gobbi/projects/gobbi/skills/principles/SKILL.md` returns `1`; `grep -c 'Twelve principles' ...` returns `0`. `.claude/skills/memorization/{SKILL.md,memory-map.md,rules.md}` and all `.claude/skills/memorization/templates/*.md` symlinks resolve to the canonical `.gobbi/projects/gobbi/skills/memorization/...` files.

[LOW|general|100] RE-TOUCH-GUARD - CLOSED. `git --no-pager diff --name-only f425c45..HEAD` does not include `principles/SKILL.md`, `.claude/CLAUDE.md`, `skills/memorization/rules.md`, or any delegation files. `HEAD` is exactly `2f86cb1`, so there is no committed `2f86cb1..HEAD` regression beyond the incomplete COD-CONS-001 remediation above. The dirty worktree has unrelated/untracked session-state/evaluation artifacts, not target-source changes.

VERDICT: REVISE

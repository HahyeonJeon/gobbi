---
name: subagent-continuation-mechanism
description: Decomposes the locked Agent-Teams subagent-continuation redesign (T1–T4) into 5 ordered executor tasks.
type: plans
scope: feature
feature: agents
status: active
created: 2026-06-07
session: a4e3b54d-3182-4193-8a42-69fce489a098
tags: [agents, delegation, orchestration, continuation, agent-teams]
task: subagent-continuation mechanism (T1–T4 bundled, Agent-Teams)
supersedes: null
superseded_by: null
task_count: 5
---

# Subagent Continuation Mechanism (Agent Teams) — Plan

## Idea anchor

Locked design: `ideation/artifacts/ideation-brief.md` (Ideation iter3 dual-system PASS). Staged design: `ideation/staging/design/subagent-continuation-mechanism.md`. Redesigns gobbi's subagent system so the manager CONTINUES a persistent teammate (Claude Code Agent Teams) across phases/steps instead of always spawning fresh — leader-chain continue within a loop; executor-chain continue iff file/feature overlap AND ≤3 consecutive tasks; assistant fresh; evaluator FORBIDDEN as teammate.

## Scope Contract reference

`ideation/artifacts/ideation-brief.md` § Scope Contract. Project gobbi / Feature agents. In scope: T1–T4 documentation + orchestration-discipline edits. Out of scope: evaluator continuation, assistant continuation, new runtime, operator-pre-check automation, creating the absent `claude` doc skill.

## Sub-tasks

| # | Sub-task | Depends on | Verification | Owner type |
|---|---|---|---|---|
| 01 | `delegation/SKILL.md`: scope "nothing inherited" to fresh spawns; add continue-vs-fresh decision table (evaluator FORBIDDEN); state F1 predicate (overlap OR same-feature-dir; cap 3); add delta-brief mechanism. | — | grep `delta-brief` + `continue` + `FORBIDDEN` in canonical `delegation/SKILL.md`; F1 predicate present | executor |
| 02 | `session.template.json` + `orchestration/SKILL.md` metadata section: teammate-aware `agents[]` schema (turns[]/continuationOf), team-config `members` discovery, separate-session transcript location, teammate token accounting, Task/Agent-hook relation, /resume+/rewind non-survival, teammate-aware F4 measurement. | 01 | grep `teammate` + `members` in canonical `orchestration/SKILL.md`; agents[] gains continuation/teammate field | executor |
| 03 | `orchestration/workflow/ideation.md` + `execution.md` (+ `orchestration/SKILL.md` if needed): leader + executor spawn-continuation choreography; two use-modes; roster/mailbox/lifecycle policy (D9); reconcile audit-trail line (ideation.md) + "fresh executor per task" line (execution.md:27). | 01, 02 | grep `continue` in both workflow docs; audit-trail + fresh-spawn lines reconciled; roster policy present | executor |
| 04 | `agents/leader.md` + `agents/executor.md` + `delegation/templates/executor.md`: continuation write-discipline (D3) — absolute worktree path on every write surface, `git -C <worktree-abs>`, re-anchor on changed rules/mistakes/scope, re-state scope + status each turn. | 01 | grep `git -C` + `absolute` in all three files | executor |
| 05 | `agents/manager.md` + `.claude/CLAUDE.md`: qualify the two "nothing inherited" assertions to point at the continuation exception (grep the F3 text, do not hard-code line numbers). | 01 | grep the (now-qualified) F3 lines in both files; `assistant.md` + `orchestration/SKILL.md:217` NOT modified | executor |

## Dependency graph

```
01 (delegation foundation: rule + delta-brief vocabulary)
 ├─> 02 (teammate-aware metadata)
 │     └─> 03 (orchestration choreography + roster/mailbox/lifecycle policy)
 ├─> 04 (continuation write-discipline in agent specs + executor template)
 └─> 05 (inheritance-assertion qualification)
```

Recommended serial order: 01 → 02 → 03 → 04 → 05. Execution runs sequentially.

**File-overlap conflict:** tasks 02 and 03 both touch `orchestration/SKILL.md` (02 = metadata section; 03 = roster policy, only if needed). Serialize 02 before 03; 03 re-reads the file fresh after 02 commits. No other cross-task file overlap.

## Verification strategy summary

Each sub-task verifies via a file-existence / grep check proving the edit landed — NOT a test. The real correctness gate is the dual-system Execution EVALUATION (Claude + Codex, all seven perspectives + Overall). Skill files under `.claude/skills/` are symlinks → Edit via canonical `.gobbi/projects/gobbi/skills/...` paths. `agents/*.md` and `.claude/CLAUDE.md` are real files.

**Manager-corrected note:** `.claude/agents/{manager,leader,executor}.md` ARE symlinks → `.gobbi/projects/gobbi/agents/{role}.md`. Only `.claude/CLAUDE.md` is a real file. Edit every skill+agent target via canonical `.gobbi/projects/gobbi/{skills,agents}/...` real file path.

## Open issues

None blocking from Planning. Carried from Ideation/Preparation (threaded into executor briefs, not blockers):
1. Citation-fidelity: give full repo-wide paths for `reconcile-session-metadata.sh` (canonical `.gobbi/projects/gobbi/skills/orchestration/scripts/`) and `reconstruct-agents.sh` (`.claude/scripts/`); never claim "missing" from a `.claude/`-only grep (mistake `false-missing-file-grep-scoped-to-wrong-dir`).
2. D5 exact-JSON-shape + teammate-rollup mechanism is design-within-bounds for task 02 (per Scope Contract); not re-opened in Planning.
3. `claude` doc-authoring skill absent (FLAG-2) — deferred; executors follow house style + the staged `claude-code-agent-teams.md` reference.

## Outcome

Executed as planned. 6 commits (tasks 01–05 + a 6th survivor-sweep task 06 added during Execution). Dual-system PASS on iter2 after survivor sweep. See `features/agents/changelogs/` for post-execution record.

{
  "name": "@gobbi/typescript-examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Example-verification harness for the gobbi `typescript` skill: extracts fenced TypeScript teaching fragments and type-checks them under one strict compatibility profile. It does not verify every runtime host or package artifact mode. Not published.",
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
